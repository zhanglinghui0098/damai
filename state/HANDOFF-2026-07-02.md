# 2026-07-02 大脉画布大改 + 阿里云短信签名验证 — Handoff

> **新 session 必读** — 2026-07-02 13:30 CST user 确认:画布总算改好了 + 阿里云通知短信验证通过 + 3 家运营商签名通过待验证。本文件记录 07-01 ~ 07-02 完整时间线 + 当前状态 + 后续计划。

---

## 1. TL;DR

| 模块 | 状态 | 备注 |
|---|---|---|
| 画布 (v2 sandbox) | ✅ **已修复, 已部署生产** | `https://damai.net.cn/sandbox/canvas` 真渲染 CanvasFlowSandbox |
| 主页画布入口 | ✅ **已替换** | Footer / Hero / StartCreating / `/canvas` redirect 都指向 `/sandbox/canvas` |
| 老画布 `/canvas/[id]` | 🟡 路由还在(暂保留) | 不再被主页入口触发;若要彻底删除需后续 commit |
| 阿里云 SMS 签名 | ✅ **3 家运营商全部通过** | 「杭州即客传媒」+ 验证码模板 `SMS_335341232` |
| 阿里云真发测试 | ✅ 06-29 13:50 OK | user 当时未收到短信,07-02 收到阿里云系统通知(见 §4) |
| GitHub 推送 | ⚠️ **网络挂** | 2 次 git push 都 `GnuTLS recv error / connect timeout`;本地 commit + 本地 deploy OK |

---

## 2. 画布修复完整时间线 (07-01 ~ 07-02)

### 2.1 06-30: Phase 3.5 chrome 4 件套 1:1 移植
- `8709229` 老画布 `.old.tsx` 备份 (3353 行, 120KB)
- `991c618` `/canvas/[id]/page.tsx` redirect → `/canvas-v2/[id]` (06-30 06:50)
- `5552265` 画布 Phase 3.5 完成:chrome 4 件套 1:1 移植 (TopBar / 脉 logo / FloatingTools / ZoomControls) + 4 类操作

### 2.2 07-01: 节点 UI 同步 + 拖线 bug 持续排查
- 6 个 commit (`1edc043` ~ `babe919`): 同步 ImageNode UI 到 text/video/audio/merge/output 5 节点
- 桌面 Chrome 拖完线消失 bug 30+ commit 未修 (BUG-REPORT-2026-07-01-DRAG-EDGE.md)
- SSR hydration 修复 (`6c3c2cd` page.tsx + `35b6ce2` 端口蓝紫发光)
- 蓝紫 edge 0.85 + 2.5px (`54f64e8`)
- snap handle `isConnectableEnd` (`2409976` `c1a7adc` `f723be6`)
- viewport 锁 2x 修复 (尝试,临时移除)
- user 决定:"画布脱线这个东西不打算继续改了"

### 2.3 07-02 05:20-07:00: 方案 2 混合架构(本文件核心)
user 拍板:画布主框架保留 React Flow v12,只替换**连接线/端口**为自研老画布(CanvasEditor.old.tsx 移植)
- `77f9029` 自研连接线系统 (方案 2 混合架构)
  - 新增: `PortDot` / `ConnectionPath` / `SelfDrawnEdge` / `NodeInteractionContext` / `PendingLineOverlay`
  - 删: `onConnect` / `onConnectStart` / `onConnectEnd` / `handleEdgesChange` / `connectionLineComponent` / `connectionMode` / `connectionRadius`
  - 全局 window mousemove + `findNearest` + `tryConnect` 自研
- `b24e8b4` 修 3 个 e2e bug: PortDot 视觉 (默认 opacity 0.32+scale 0.65 → 0.7+0.95+2px 黑 outline) / findNearest 阈值 (world → screen 距离) / 隐形 Handle 注册
- 走 NAS dev 3001 + cloudflared tunnel,user 自验 (不部署 ECS)

### 2.4 07-02 10:00-11:44: sandbox 路由独立 (user 主动决策)
user 10:00 决定:**v2 sandbox 路由完全独立**,不污染生产 `/canvas/[id]`
- `57c637b` 新加 `/sandbox/canvas` 路由
  - `app/sandbox/canvas/page.tsx` (1KB): next/dynamic + ssr:false
  - `app/sandbox/canvas/CanvasFlowSandbox.tsx` (1183 行 / 38KB): 5 个低风险改动版,localStorage 存数据 (`damai:canvas-v2:r2:sandbox`),不接 production API
  - `app/sandbox/canvas/CanvasFlowSandbox.module.css` (13KB)
- `669d2c4` middleware 加 `/sandbox` 前缀到公域 (本地不需登录可访问 sandbox 路由)

### 2.5 07-02 12:58-13:30: dev HMR 验证 + dev/cloudflared 端口修正
- 发现 dev 跑 3001 (3000 被 systemd 占), cloudflared 之前配 3001 → 实际指 3000 → 切到 3001
- 老画布 Syntax Error 是 HMR stale 状态,重启 dev 后 `/canvas/test` 200
- localhost:3001 `/sandbox/canvas` 200 (middleware 生效)

### 2.6 07-02 13:30 deploy 到 ECS(第一次)
- `f60a501 chore(deploy+state): 07-02 pending 收口` 推 origin master
- deploy-to-ecs.sh 全跑通: tar 51M → 上传 → 备份 → 解压 → npm install → next build (1024M heap) → pm2 delete + start
- PM2 PID 140212 online 57.8MB
- `damai.net.cn` HTTP 200, `/sandbox/canvas` HTTP 200 (12678B, x-nextjs-cache HIT)
- 老画布 `/canvas/test` 也 HTTP 200 (6614B)

### 2.7 07-02 13:45 user 反馈 + 修主页入口
- **user 反馈**:"从大脉首页退回,再进画布工具,变成之前的那个版本 (老画布)"
- **user 诊断**:"是不是我主页的那个画布的入口还没有替换掉的原因"
- **确认根因**: 主页 4 处入口仍指 `/canvas/new` 老画布
  - `components/SiteFooter.tsx:13` Footer 画布链接
  - `components/HeroAgent.tsx:247` "进画布" 按钮
  - `components/StartCreating.tsx:27` 画布 tile
  - `app/canvas/page.tsx:6` redirect
- **修改 4 处** → 全部指向 `/sandbox/canvas`
- `5f0d362 fix(home): 主页画布入口替换为 /sandbox/canvas v2` (4 files / 4+/4-)
- GitHub push 失败 (GnuTLS / connect timeout),但 deploy 用本地 tar 不依赖 push
- 13:30 deploy 完成 (PID 141464 online 57.9MB, damai.net.cn HTTP 200 53107B)
- user 强刷可验证 4 个入口

---

## 3. 整体任务推进状态

### 3.1 P0 (核心链路)
| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| P0 #1 | SWAS 部署链路 | ✅ | `scripts/deploy-to-ecs.sh` admin+sudo 路径 |
| P0 #2 | stub curl 通过 | ✅ | `provider:"stub"`, devCode 返前端 |
| P0 #3 | .env.local 4 个真值 | ✅ | DAMI_SMS_REAL + 4 个 ALIYUN_SMS_* |
| P0 #4 | tenantId 走 session cookie | ✅ | verify-cookie upsert Bitable user |
| P0 #5 | 阿里云真发测试 | 🟡 | 06-29 13:50 route OK,user 当时未收短信,07-02 收阿里云系统通知 (见 §4) |

### 3.2 P1 (生产化)
| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| P1 #1 | 画布交互稳定性 | ✅ | 混合架构 + 主页入口全部替换 |
| P1 #2.1 S1-S3 | 飞书项目表 + MyProjects 真数据源 | ✅ | commit 55ab837 + 29122a0 |
| P1 #2.1 S4 | dashboard ProjectsTab 接真 Bitable | ⏳ | 仍 mock-data-workbench |
| P1 #3 | NAS 备份脚本改真 IP + 飞书告警 webhook | ⏳ | NAS_BACKUP_HOST 还是 placeholder |

### 3.3 P2 (扩展)
- ⏳ P2 #8 画布脱线老画布 fallback (现改用混合架构,降级方案)
- ⏳ AI 视频 SaaS 全面上线 (B 端先)
- ⏳ 顾家家居 (宁波) 客餐厅 2026 短视频项目 (B 端)

### 3.4 待 user 决策
- [ ] **GitHub push 失败**: NAS → github.com 网络问题,user 决定是切 SSH key 还是要 SSH 配置 (push 失败不影响 deploy)
- [ ] **未改的画布入口**(二级页面): dashboard / case / templates / workbench 项目链接 (user 反馈范围只在主页)

---

## 4. 阿里云 SMS 通知 (2026-07-02)

### 4.1 user 反馈 (07-02 13:45)
- "今天阿里云给我发短信了,通知短信验证应该是这一块是通过了"
- "现在三家运营商都是签名已经通过了,然后就是待验证"

### 4.2 状态对照
| 阶段 | 状态 |
|---|---|
| 阿里云账号开通 + 实名认证 | ✅ UID 1148781509211780 (主账号) |
| 短信服务 dysmsapi (杭州 region) | ✅ 已开通 |
| 主账号 AK | ✅ `LTAI5t6k3vqta8v3GYSmDbCx` (06-27 rotate 后,OSS+SMS 同源) |
| 短信签名「杭州即客传媒」 | ✅ 审核通过 |
| **3 家运营商签名报备** | ✅ **07-02 通过** (user 收到通知) |
| 验证码模板 `SMS_335341232` | ✅ 审核通过 |
| DAMI_SMS_REAL=true | ✅ .env.local 已配 |
| 06-29 13:50 真发测试 | ✅ 链路通,但 user 当时未收短信 (因运营商报备未完成) |
| **当前阶段** | 🟡 **待验证**: user 试发到 3 个不同运营商手机号,确认签名实际能送达 |

### 4.3 接下来要做的 (按 user 反馈"看看接下来怎么弄")
1. **user 选 3 个测试手机号**(3 家运营商各 1 个: 移动 / 联通 / 电信)
2. 给我手机号 (或 user 自己 curl)
3. 我 curl `POST /api/auth/send-code` 分别发到 3 个手机号
4. 3 个手机号都收到 → 真发 + 验证完整链路通 → 收口 P0 #5
5. 同步把 BACKLOG `⏳ user 收 SMS 验证码` 改 ✅,记录 3 个手机号 + 到达时间

### 4.4 需要的 user 输入
- [ ] 3 个测试手机号 (或者 user 自己跑 send-code,告诉我到达结果)
- [ ] 是否需要我改 P1 短信频率限制 (目前是 60s 1 次 / 1h 5 次,3 家用 1 个手机号就够,跨号不需要改)

---

## 5. 关键文件 + 行号 (新 session 速查)

| 文件 | 作用 | 关键位置 |
|---|---|---|
| `app/sandbox/canvas/page.tsx` | sandbox 路由入口 | 1KB, next/dynamic + ssr:false |
| `app/sandbox/canvas/CanvasFlowSandbox.tsx` | sandbox 画布核心 | 1183 行 / 38KB, prototype 设计, localStorage 存 |
| `app/canvas/[id]/CanvasFlowEditor.tsx` | 老画布 React Flow 版 | 2532 行 / 90KB,混合架构 (自研 PortDot) |
| `app/canvas/[id]/CanvasEditor.old.tsx` | 老老画布 (自研 SVG, Phase 4 备份) | 3353 行 / 120KB,**未删** |
| `components/SiteFooter.tsx` | 主页 Footer | :13 画布链接 |
| `components/HeroAgent.tsx` | 主页 AI 助手 | :247 进画布按钮 |
| `components/StartCreating.tsx` | 主页"开始创作"区 | :27 画布 tile |
| `app/canvas/page.tsx` | /canvas 列表 redirect | :6 redirect → `/sandbox/canvas` |
| `middleware.ts` | 鉴权 + 多租户 | :36 `/sandbox` 公域前缀 |
| `lib/sms.ts` | 阿里云 SMS 客户端 | dysmsapi SDK |
| `app/api/auth/send-code/route.ts` | 发送验证码 API | provider: stub / aliyun switch |
| `app/api/auth/verify-code/route.ts` | 验证 + 登录 | 收 P0 #5 最后一步 |
| `scripts/deploy-to-ecs.sh` | 一键部署 ECS | admin + sudo + 1024M heap |

---

## 6. 后续计划 (按优先级)

### 6.1 立即 (今天 14:00-18:00)
- [ ] **P0 #5 收口**: 3 个手机号真发测试 → 收口阿里云 SMS 真发模式
- [ ] **GitHub push 修复**: user 决定切 SSH key / SSH 配置 / 用代理 (NAS 推 GitHub 失败)
- [ ] user 强刷验证 4 个主页入口 → /sandbox/canvas

### 6.2 短期 (本周)
- [ ] **P1 #2.1 S4**: dashboard ProjectsTab 接真 Bitable (替换 mock-data-workbench)
- [ ] **P1 #3**: NAS 备份脚本改真 IP + 飞书告警 webhook URL
- [ ] **决策: 二级页面入口改不改**: dashboard / case / templates / workbench 全部改 /sandbox/canvas,还是保留老画布?等 user 决定
- [ ] **老画布路由 `/canvas/[id]` 处理**: 删 / 保留 / 改名 (待 user 决策)

### 6.3 中期 (下周)
- [ ] **顾家家居项目启动**: B 端第一单,客餐厅 2026 短视频项目 (365 天周期,4A 风格)
- [ ] **P2 画布迭代**: 自研 PortDot + 自研 Edge 优化 (有 user 反馈再迭代)
- [ ] **大脉 v1 正式发布**: 域名 damai.net.cn, PM2 online 稳定, 上线公告

### 6.4 长期 (本月)
- [ ] AI 视频 SaaS 全功能上线 (B 端先)
- [ ] Bitable 接项目表 + 节点表 + canvas 从 localStorage 改飞书
- [ ] 升级 ECS 2C/4G (当前 896Mi 经常 OOM)
- [ ] CDN 接 OSS + Sentry 监控

---

## 7. 部署命令速查

```bash
# 部署生产 (从 NAS 推 ECS, 不需要 push GitHub)
cd /opt/data/projects/damai
git add <specific>   # 不要 -A,按 memory 06-29 commit 范围纪律
git commit -F /tmp/commitmsg.txt
bash scripts/deploy-to-ecs.sh

# 看生产状态
ssh -i /opt/data/home/.ssh/damai-ecs admin@47.96.128.172 'pm2 list'
curl -sIL https://damai.net.cn/sandbox/canvas
curl -s https://damai.net.cn/api/auth/send-code -X POST -H "Content-Type: application/json" -d '{"phone":"15925670098"}'

# 本地 dev (HMR)
cd /opt/data/projects/damai
nohup npm run dev > /tmp/next-dev.log 2>&1 &   # 默认 3000,被占跳 3001
nohup cloudflared tunnel --url http://localhost:3001 --no-autoupdate --protocol http2 > /tmp/cf-bg.log 2>&1 &
```

---

## 8. 教训 (本轮新增)

1. **端口一致性**: 之前 dev 3000 → 3001 跳端口时,cloudflared 仍指 3000,公网 URL 530。要同步 kill + restart。
2. **middleware HMR**: 11:43 改 middleware `/sandbox` 公域后,dev 必须重启 (next dev middleware HMR 有时不重读)。
3. **deploy 不依赖 push**: GitHub 网络挂时,本地 tar + cat|ssh 部署仍能跑通。**但** ECS 跟 master 有差,user 后续 push 才能补。
4. **不能信 "deploy 完成"**: 必须自己 curl 验证 HTTP 200 + 看 PM2 + 看 build 日志 (memory 06-29 "完事了不可信")。
5. **HMR stale 误判 Syntax Error**: 之前 `/canvas/test` 404 不一定是真错,dev 重启后 200。**先 restart 再 debug**。
6. **画布 chrome 4 件套 1:1 移植**: sandbox CanvasFlowSandbox 跟老 React Flow 画布视觉**很像**,user 容易混淆。要看 footer / 操作方式 (sandbox v2) / edge selfDrawn 标识区分。

---

**最后更新**: 2026-07-02 13:30 CST
**作者**: Hermes Agent (MiniMax-M3) + user
**关联**: STATUS.md / BACKLOG.md / BUG-REPORT-2026-07-01-DRAG-EDGE.md
